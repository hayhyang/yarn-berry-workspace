// import Feature24Icon from '@icon/feature/24'

interface IconProps {
  size: string;
}

const Icon = (props: IconProps) => {
  // <Feature24Icon  />
  const { size } = props;
  return <span></span>;
};

export default Icon;

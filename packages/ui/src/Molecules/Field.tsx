import React from "react";

interface FieldProps {
  labelProps: any;
  inputProps: any;
}

const Field = ({ labelProps, inputProps }: FieldProps) => {
  return <div></div>;
};

export default Field;
